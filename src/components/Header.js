import React from "react";
import { Box, Typography } from "@mui/material";

const Header = () => {
  return (
    <Box className="header-container">
      <span className="logo">Splitz</span>
    </Box>
  );
};

export default Header;
